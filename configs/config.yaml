app:
  env: development
  port: "8080"
  log_level: info

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: ela
  ssl_mode: disable
  timezone: UTC

jwt:
  secret: change-me-in-production
  expiry_seconds: 3600
